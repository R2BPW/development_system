# План рефакторинга development_system

## Диагноз — почему система неработоспособна

| # | Проблема | Где |
|---|----------|-----|
| 1 | Формат `душа.scm` не совпадает с `читать-душу` → системный промпт пустой | dusha.lisp |
| 2 | `%обр-старт` вызывает `send-message` сам, возвращает `nil` → нарушен контракт | команды.lisp |
| 3 | Кнопки «Запустить» → `/запустить` без аргументов → «Использование:…» | команды.lisp |
| 4 | LLM-запросы без таймаута блокируют polling loop | llm.lisp |
| 5 | `*llm-key*` вычисляется при загрузке → nil если ключ задан после | llm.lisp |
| 6 | История диалога без ограничения длины → LLM error | история.lisp |
| 7 | `загрузить-все-потоки` не заполняет `*активные-потоки*` → `/состояние` пустое | потоки.lisp |
| 8 | Тесты без моков требуют живые токены → CI невозможен | тесты.lisp |
| 9 | Смешанные HTTP: dexador в мастере, drakma в потоках | — |

---

### Task 1: Фаза 1 — Запускаемость

- [x] 1.1 Исправить `душа.scm` ↔ `читать-душу` — привести формат файла к ожидаемому или наоборот, добавить проверку при старте
- [x] 1.2 Единый контракт обработчиков — все `%обр-*` возвращают `(values строка &optional reply-markup)`, IO только в `%dispatch`
- [x] 1.3 Таймаут LLM — `dexador` timeout 60 сек, `handler-case` → «[таймаут LLM]»
- [x] 1.4 Ленивое чтение ключа — `%llm-key` функция вместо переменной при загрузке
- [x] Тесты фазы 1 — `душа->системный-промпт` на fixture, `%обр-старт` возвращает строку

---

### Task 2: Фаза 2 — Инъекция зависимостей (моки)

- [x] 2.1 Подменяемые функции — `*api-post-fn*`, `*llm-fn*` в telegram.lisp и llm.lisp
- [x] 2.2 Макрос `с-моками` для тестов
- [x] Тесты фазы 2 — `обработать-команду` с заглушкой IO, свободный текст → LLM-mock

---

### Task 3: Фаза 3 — Юнит-тесты (без IO)

- [x] Тесты `%split-words` — пустая строка → nil; "a  b" → ("a" "b")
- [x] Тесты `%cmd-args` — "/запустить эхо задача" → cmd + args
- [x] Тесты `%кнопка->команда` — точный матч кнопок
- [x] Тесты `%pkg-name` — "эхо" → "ПОТОК-ЭХО"
- [x] CI entrypoint `тесты/run-ci.sh`

---

### Task 4: Фаза 4 — Исправить состояние потоков и историю

- [x] 4.1 Регистрация потоков в `*активные-потоки*` при загрузке
- [x] 4.2 Ограничение истории — `*макс-история*` = 20
- [x] Тесты фазы 4 — `добавить-сообщение` × 25 → длина ≤ 20, `/состояние` непустой

---

### Task 5: Фаза 5 — Юзабилити

- [x] 5.1 Интерактивный `/запустить` — без аргументов → inline-клавиатура
- [x] 5.2 Унификация HTTP-клиента — все на dexador, убрать drakma
- [x] 5.3 Фильтр доступа — `chat-id ≠ *admin-chat-id*` → «Доступ запрещён»
- [x] Тесты фазы 5

---

### Task 6: Фаза 6 — Интеграционные тесты мастера

- [x] Интеграционные тесты в `тесты/master.lisp` — все сценарии из таблицы

---

### Task 7: Фаза 7 — Тесты потоков (требуют API-ключ)

- [ ] Тесты потоков в `тесты/streams.sh` — эхо, уточнение, выполни-код, реакт, порождатель

---

### Task 8: Фаза 8 — Рефакторинг кода

- [ ] 8.1 Обработка ошибок — единый паттерн `handler-case`, убрать `ignore-errors`
- [ ] 8.2 Логирование — `log/info`, `log/error` с timestamp
- [ ] 8.3 Дедупликация потоков — питон.lisp и исполнитель.lisp → один

---

### Task 9: Фаза 9 — Smoke-тесты (живой бот)

- [ ] Smoke-тесты в `тесты/bot.sh` — /старт, /потоки, /запустить эхо ping, свободный текст

---

## Edge cases

| Сценарий | Проверка |
|----------|----------|
| Offset persistence | рестарт → старые сообщения не дублируются |
| Rate limit 429 | api-post → nil → poll продолжается |
| Broken stream | синтаксическая ошибка → лог "FAIL", остальные грузятся |
| Nil text guard | `(send-message 123 nil)` → ничего не отправляется |

---

## CI entrypoint

**`тесты/run-ci.sh`**: запускает unit + master (без API-ключей), код выхода = число ошибок.

---

## Порядок выполнения

```
Ф1 запускаемость → Ф2 моки → Ф3 юниты → Ф4 состояние → Ф5 юзабилити → Ф6 интеграция → Ф7 потоки → Ф8 рефакторинг → Ф9 smoke
```

Каждая фаза коммитируется отдельно. После Ф3 CI без токенов должен быть зелёным.

---

## Не трогать

- `мастер/потоки/*.lisp` — потоки работают
- `мастер/душа.scm` — данные
- `docs/` — статьи
- `комбо.lisp` — до появления команды `/комбо`
