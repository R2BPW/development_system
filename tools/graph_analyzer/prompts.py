"""LLM-промпты для граф-аналитика."""

SYSTEM = """Ты — аналитик бизнес-процессов. Тебе дан граф процесса и Django-модели.
Твоя задача — найти конкретные проблемы. Отвечай ТОЛЬКО валидным JSON.
Не выдумывай проблемы которых нет. Каждая находка — реальная, с конкретным узлом графа.
Severity: critical (нужно исправить до релиза), warning (желательно исправить), info (замечание)."""

COMPENSATION_PROMPT = """
Граф процесса:
{graph}

Django-модели:
{models}

Информация о error-узлах и предшествующих шагах:
{error_paths}

Задача: найди пробелы в компенсации ошибок.
Для каждого error-узла (ERR0, ERR1, ERR2 и т.д.) проверь:
1. Какие шаги уже выполнились до этого error-узла?
2. Что изменилось в БД (статусы, версии, FK)?
3. Компенсирует ли error-ветка эти изменения?

Типичные пробелы: создали записи → ошибка → записи остались в БД;
изменили статус → ошибка → статус не откатился.

Отвечай JSON:
{{
  "findings": [
    {{
      "node": "ERR1",
      "category": "compensation_gap",
      "description": "Конкретное описание что не откатывается",
      "preceding_steps": ["E20.1", "E20.2"],
      "recommendation": "Что добавить в компенсацию",
      "severity": "critical"
    }}
  ]
}}
"""

EDGE_CASES_PROMPT = """
Граф процесса:
{graph}

Django-модели:
{models}

Задача: найди крайние случаи и неожиданные состояния.

Проверь:
1. Граничные входные данные: пустые списки (containers=[]), null-значения,
   даты в прошлом, несуществующие FK
2. Комбинации полей модели которые граф не рассматривает:
   например is_moved=True при status=in_route одновременно
3. Параллельные вызовы: что если add_tour вызвали дважды для одного маршрута?
4. Частичное выполнение: что если процесс прервался на середине предыдущего запуска?
5. Узлы где отсутствует явная проверка входных данных

Отвечай JSON:
{{
  "findings": [
    {{
      "node": "E38.5",
      "category": "unexpected_state",
      "description": "Конкретная комбинация полей или ситуация",
      "model": "WasteSiteVisitedContainers",
      "fields_involved": ["is_moved", "status"],
      "recommendation": "Что проверить или добавить",
      "severity": "warning"
    }}
  ]
}}
"""

REACHABILITY_PROMPT = """
Граф процесса:
{graph}

Недостижимые узлы (вычислено алгоритмически):
{unreachable}

Задача: найди логические проблемы достижимости.

1. Есть ли узлы которые недостижимы при нормальном потоке?
2. Есть ли ветки где условие никогда не может быть истинным
   (например, проверка X после шага где X уже гарантированно выполнен)?
3. Есть ли циклы которые могут не завершиться?
4. Все ли happy-path ведут к терминальным узлам?

Отвечай JSON:
{{
  "findings": [
    {{
      "node": "F2",
      "category": "unreachable_branch",
      "description": "Почему ветка недостижима",
      "path": "E25 -> F0 -> F1 -> F2",
      "recommendation": "Убрать ветку или пересмотреть логику",
      "severity": "info"
    }}
  ]
}}
"""
